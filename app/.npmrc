# React Native / Expo expects a flat(ish) node_modules so peer deps resolve.
# `install-strategy=nested` breaks peer resolution (e.g. ts-jest → jest-util),
# and can create duplicate native modules that break prebuild/autolinking.
#
# Keep default npm behavior (hoisted) and surface peer issues early.
